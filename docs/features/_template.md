# {기능 이름}

## 개요

{기능 설명. 인증 필요 여부, 접근 대상 등 명시}

---

## 화면 구성

- {UI 요소 1} (`label: ...` 또는 `role: ...`)
- {UI 요소 2}

---

## {기능} 플로우

1. {사용자 행동}
2. {시스템 반응}
3. {결과}

**API 연동:**
- Endpoint: `{METHOD} {path}`
- Request: `{ ... }`
- Response: `{ ... }`

---

## 예외 처리

- {조건}이면 `{에러 메시지}` 표시
- {조건}이면 `{동작}` 처리
